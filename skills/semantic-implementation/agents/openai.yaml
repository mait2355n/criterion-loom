interface:
  display_name: "Semantic Implementation"
  short_description: "Meaning-first requirements, implementation, LLM reviewer, and final human-review audit"
  default_prompt: "Use $semantic-implementation to clarify meaning, intent, requirements, plan risks, verification, intermediate LLM review when useful, and final human-review evidence."
dependencies:
  tools:
    - type: "mcp"
      value: "semantic_guard"
      description: "Local semantic-guard MCP server for requirement, plan, diff, finish, LLM reviewer, and acceptance bundle audits"
      transport: "stdio"
policy:
  allow_implicit_invocation: true

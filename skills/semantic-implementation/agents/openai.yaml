interface:
  display_name: "Loom Guide"
  short_description: "Meaning-first audit routing for Codex work"
  default_prompt: "Use $semantic-implementation to route non-trivial Codex work through meaning-first audits, document-expression checks, verification evidence, and final human-review boundaries."
dependencies:
  tools:
    - type: "mcp"
      value: "semantic_guard"
      description: "Local semantic-guard MCP server for meaning-first audit, convention checks, trace reports, reviewer material, and acceptance bundles"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
